name: source-list preflight

on:
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Validate the 125-site source list
        run: |
          python3 scripts/validate_source_list.py

      - name: Upload source-list evidence
        uses: actions/upload-artifact@v4
        with:
          name: aether-source-list-${{ github.run_id }}
          path: news_websites.csv
          retention-days: 14
