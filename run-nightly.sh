#!/bin/bash
# Retained only to stop obsolete local scheduler installations safely.
set -euo pipefail
echo "Local nightly runs are disabled. Use GitHub Actions nightly-article-pipeline.yml." >&2
exit 1
